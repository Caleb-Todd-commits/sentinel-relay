# Build Prompts

These prompts are meant for Codex/Cursor/Claude/ChatGPT coding agents.

## Prompt 1 — Baseline

Create a stable baseline for Sentinel Relay using this repo structure. Do not overbuild. Make the app run, create placeholder pages, add sample data, add agent folders, and document the architecture.

## Prompt 2 — Mock Vertical Slice

Build a mocked incident workflow for "Possible API Key Exposure After Friday Deploy". Use structured TypeScript types. Update the War Room UI to show agent messages, evidence, timeline, approval gate, remediation tasks, and report preview. Add replay mode.

## Prompt 3 — Collaboration Provider

Create a provider abstraction with MockCollaborationProvider and BandCollaborationProvider. Keep mock mode fully working. Add safe fallback if Band credentials are missing.

## Prompt 4 — Real Band Integration

Connect the Band provider to real Band rooms and messages. Make sure at least three agents communicate through Band during the actual workflow.

## Prompt 5 — Polish

Improve visual clarity, final report quality, demo reliability, README, and submission materials.
